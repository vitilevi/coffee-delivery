export const size = {
  sm: 425,
  md: 768,
  lg: 1024,
  dt: 1200,
  xl: 1440
};

export const breakpoints = {
  sm: `(min-width: ${size.sm}px)`,
  md: `(min-width: ${size.md}px)`,
  lg: `(min-width: ${size.lg}px)`,
  dt: `(min-width: ${size.dt}px)`,
  xl: `(min-width: ${size.xl}px)`
};
